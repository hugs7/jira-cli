#!/usr/bin/env sh
# One-liner manual install for macOS + zsh.
# Pipe through `sh` — fetches install-mac-zsh.sh from main and runs it.
curl -fsSL https://raw.githubusercontent.com/hugs7/jira-cli/main/scripts/install-mac-zsh.sh | sh
